/// Push (FCM) et SMS (Africa's Talking) — responsabilité Jean.
class NotificationService {
  Future<void> init() async {
    throw UnimplementedError('À brancher sur Firebase Messaging');
  }

  Future<void> requestPermission() async {
    throw UnimplementedError('À brancher sur Firebase Messaging');
  }
}
